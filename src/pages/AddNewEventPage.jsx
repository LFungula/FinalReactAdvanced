import { Heading } from "@chakra-ui/react";
import { useSubmit } from "react-router-dom";
import { useToast } from "@chakra-ui/react";

export const AddNewEventPage = () => {
  return <Heading> Your new event here!</Heading>;
};
